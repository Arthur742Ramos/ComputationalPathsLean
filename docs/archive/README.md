# Historical archive

This directory holds historical audits, one-off investigation notes, generated
run outputs, and tracked scratch summaries that are useful for provenance but
are not active project entry points.

Canonical project documentation lives in `docs/ARCHITECTURE.md` and
`docs/axioms.md`.
